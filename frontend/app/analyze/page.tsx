import AnalyzeComponent from "@/components/analysis/AnalyzeComponent";

export default function AnalyzePage() {
    return (
        <main className="flex flex-col items-center justify-center min-h-screen p-4 sm:p-8 bg-gray-50">
            <AnalyzeComponent />
        </main>
    );
}